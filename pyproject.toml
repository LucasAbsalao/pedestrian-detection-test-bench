[project]
name = "computer-vision"
version = "0.1.0"
description = ""
authors = [
    {name = "Your Name",email = "you@example.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas (>=3.0.2,<4.0.0)",
    "yt-dlp (>=2026.3.17,<2027.0.0)",
    "ultralytics (>=8.4.61,<9.0.0)",
    "tqdm (>=4.68.3,<5.0.0)",
    "pyaudio (>=0.2.14,<0.3.0)",
    "opencv-contrib-python (>=5.0.0.93,<6.0.0.0)",
    "pyyaml (>=6.0.3,<7.0.0)",
]

[tool.poetry]
packages = [{include = "computer_vision", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.0.3,<10.0.0)"
]
